#!/bin/sh

# This script is just shorthand for getting a shell with your app's environment
# set up.

. $PWD/cz/environment

echo "$CZ_APP_NAME ($CZ_APP_VERSION) opening console for $ENVIRONMENT..."

# You might wonder why we choose to run /bin/sh here instead of a rails console.
# The answer is that while it's simple to run a rails console from a shell, it's
# awkward to do the reverse, if we wish to.
/bin/sh
